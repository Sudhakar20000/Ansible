- name: loop for install
  hosts: frountend
  become: true
  tasks:
    - name: install packages
      package:
        name: "{{item}}"
        state: present
      loop:
        - git
        - nginx